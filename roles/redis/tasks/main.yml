- name: Install Redis Repo
  ansible.builtin.yum:
    name: https://rpms.remirepo.net/enterprise/remi-release-8.rpm
    state: installed

- name: Enable & Install Redis
  ansible.builtin.dnf:
    name: @redis:remis-6.2
    state: installed